<?xml version="1.0" encoding="UTF-8"?>
<!-- Legacy compatibility descriptor. Gradle is the canonical PlexonBackpacks build. -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.zpkdxgames</groupId>
    <artifactId>PlexonBackpacks</artifactId>
    <version>2.0.0</version>
    <name>PlexonBackpacks</name>
    <description>Premium item-safe tiered backpack storage for the Plexon ecosystem.</description>
    <url>https://github.com/ZpkDxGames/PlexonBackpacks</url>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <paper.version>26.2.build.121-stable</paper.version>
        <plexoncore.version>2.0.4</plexoncore.version>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paper.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.zpkdxgames</groupId>
            <artifactId>PlexonCore</artifactId>
            <version>${plexoncore.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>PlexonBackpacks-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
            </plugin>
        </plugins>
    </build>
</project>